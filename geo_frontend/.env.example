# URL base del backend FastAPI
VITE_API_URL=http://localhost:8000

# Espejo del toggle AUTH_ENABLED del backend.
# Mientras el backend tenga auth deshabilitada, dejar en false
# para no bloquear pantallas con login.
VITE_AUTH_ENABLED=false
